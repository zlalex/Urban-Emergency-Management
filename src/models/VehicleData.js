export default class {
  constructor(
    numeric = '',
    datum = '',
    driver = '',
    fellow = '',
    company = ''
  ) {
    this.numeric = numeric
    this.datum = datum
    this.driver = driver
    this.fellow = fellow
    this.company = company
  }
}